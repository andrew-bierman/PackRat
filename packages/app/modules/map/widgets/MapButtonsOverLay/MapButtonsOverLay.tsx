import React, { type FC } from 'react';
import { type MapButtonsOverlayProps } from './model';

export const MapButtonsOverlay: FC<MapButtonsOverlayProps> = ({ children }) => {
  return <>{children}</>;
};
